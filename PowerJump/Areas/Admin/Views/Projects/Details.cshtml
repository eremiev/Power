@model PowerJump.Models.Project

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Project</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.ProjectLocales.FirstOrDefault().Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ProjectLocales.FirstOrDefault().Title)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.ProjectLocales.FirstOrDefault().Description)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ProjectLocales.FirstOrDefault().Description)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Date)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Date)
        </dd>
        <dd>
            @if (Model.Photos.Count > 0)
            {
                <img src="@Url.Content(Model.Photos.FirstOrDefault().Path)" alt="Image" width="100" height="100" />
            }
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.GalleryId }) |
    @Html.ActionLink("Back to List", "Index")
</p>
